    <%- include("./includes/head.ejs") %>
    <link rel="stylesheet" href="/css/home.css">
  </head>

  <body>
    <%- include("./includes/navigation.ejs") %>
    <h1><%= pageTitle %></h1>
      <% if (ep.length > 0) { %>
        <% ep.forEach(item => { %>
          <h2>Folge: <%= item.title  %>!</h2>
        <% }); %>
      <% } else { %>
        <h2>keine Daten</h2>
      <% } %>
  </body>
</html>
